# CLAUDE.md (proyecto: comidas)

> La fuente de verdad de cómo trabajar en este proyecto es **[`../CLAUDE.md`](../CLAUDE.md)**
> (en la raíz del repo). Léela primero.

Este proyecto usa el sistema **PM x10 (estructura V3)**:

- Conocimiento transversal → `docs/general/` (empieza por `docs/general/PROJECT_KNOWLEDGE.md`).
- Área Producto → `docs/producto/` (`sprint.md`, `lessons.md`, `qa.md`, `inbox.md`, `features/<feature>/`).
- Estado del PM → `pm/` (no editar a mano; lo gestiona `/pm`).
- Dashboard → `python3 dashboard/bridge.py` → http://localhost:7700
- Wiki de empresa → `docs/general/wiki/` (alimentada desde `raw/` vía `/wiki`).

Reglas de coding y disciplina LLM: ver `.cursorrules` y la skill global
`rul-llm-coding-discipline`. Reglas de entornos (dev/prod) y comandos PM:
en la raíz `CLAUDE.md`.
